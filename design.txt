project structure:
    --_tests // unit test using gtest framework
    --batch // batch files to test
    --includes
        --binary_files // to store data in binary format
        --linked_list // linked list implementation
        --map // map implementation
        --parser // parser for sql statement, using state machine
        --shunting_yard // shunting yard algorithm for infix to postfix
        --sql // sql statement implementation
        --table // to store data in table format
        --tokenizer // to read sql statement and tokenize it
    --command_line.cpp // the main entry of the program in cmd
    --design.txt
    --CMakeLists.txt
    --main.cpp // the main entry of the program in gui
    --README.md

// TODO